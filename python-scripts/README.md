# Collection of python scripts for various administrative tasks

## Pre-requisites
Install python pre-requisits first
```
sudo pip install -r requirements.txt
```
*NOTE: you can also do this in your virtualenv as needed*
